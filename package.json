{
	"name": "socially",
	"version": "1.0.0",
	"private": "true",
	"workspaces": [
		"client",
		"server"
	],
	"scripts": {
		"client": "cd client && npm start",
		"server": "cd server && npm run dev",
		"dev": "concurrently \"npm run server\" \"npm run client\"",
		"server:prod": "cd server && npm run start",
		"build:client-win": "cd client && npm run build && cd .. && rmdir ./server/build && move ./client/build server/ ",		
		"start-win": "npm run build:client-win; npm run server:prod",
		"start": "npm run build:client && npm run server:prod"
	},
	"devDependencies": {
		"concurrently": "^8.2.2"
	}
}
